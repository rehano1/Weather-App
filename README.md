 
1. write a function #fethData that fetches the weather data
2. On submit, there should be all fetchData, Select Data, Display Data
3. write a function #selectData which specifies the data
4. write a function that #displayData take that selected data and display it
5. make an input in which you can add city names


1. write a function that takes the input if there is no space in the letters then ok if there is space change the pattern so that it can be requested

Display the data and write on click funtion to invoke the fetch data which will ultiately do all the work

write a loop function, it will take the object and iterated over it creating li elements